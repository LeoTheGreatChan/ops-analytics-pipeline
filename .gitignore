# Python
__pycache__/
*.pyc
*.pkl
.venv/
venv/

# Environment / secrets
.env
*.env

# OS
.DS_Store

# Raw dataset (third-party, not redistributed — see data/raw/README.md)
data/raw/*.csv

# Note: data/processed/findings_prior_snapshot.json is intentionally NOT
# gitignored. It's committed programmatically via scripts/github_commit.py
# whenever a refresh finds a material change, this is what lets the
# change-detection gate survive Render's ephemeral filesystem across cold
# starts. See github_commit.py for the full reasoning.

# Editor
.vscode/
